"use client";

// This component is no longer needed since we removed default questions
// Keeping it as a placeholder in case we need it later
export default function QuestionInitializer() {
  return null;
} 